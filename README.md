# vibe-rt

The minimal `no_std`, libc-free Linux userspace runtime shared by vibeOS
programs.

It currently provides process entry, byte-preserving arguments and environment,
console output, filesystem mounting, pause, and process exit on x86_64 Linux.
